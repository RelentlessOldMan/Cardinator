# Normalize line endings; let Git decide per file, store LF in the repo.
* text=auto

# Treat rendered images as binary (no EOL conversion / diffing).
*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
